<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-latest.xsd">

    <!--
        Order Service — order_items table
        Author: tovstukha
        Line items for each order.
        unit_price is frozen at purchase time — immune to future product price changes.
        onDelete CASCADE on order_id: deleting an order removes all its items.
        onDelete RESTRICT on product_id: prevents deletion of products still in orders.
    -->
    <changeSet id="006-create-order-items-table" author="tovstukha">
        <preConditions onFail="MARK_RAN">
            <not><tableExists tableName="order_items"/></not>
        </preConditions>
        <comment>Create order_items table for Order Service</comment>

        <createTable tableName="order_items">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false" primaryKeyName="PK_order_items"/>
            </column>
            <column name="order_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="product_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="quantity" type="INT">
                <constraints nullable="false"/>
            </column>
            <column name="unit_price" type="DECIMAL(12,2)">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="order_items" baseColumnNames="order_id"
                referencedTableName="orders" referencedColumnNames="id"
                constraintName="FK_order_items_order"
                onDelete="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="order_items" baseColumnNames="product_id"
                referencedTableName="products" referencedColumnNames="id"
                constraintName="FK_order_items_product"
                onDelete="RESTRICT"/>

        <createIndex tableName="order_items" indexName="idx_order_items_order_id">
            <column name="order_id"/>
        </createIndex>

        <!-- ============================================================== -->
        <!-- SYSTEM REGISTRATION — DO NOT MODIFY                            -->
        <!-- ============================================================== -->
        <sql>
            INSERT INTO dbo.tables (db_, name, type, author, version, service, description, column_count, created_at, last_update_at)
            VALUES ('liquibase_lab', 'order_items', 'DATA_TABLE', 'tovstukha', 1, 'order_service',
                    'Order line items. unit_price fixed at purchase time. CASCADE on order_id.',
                    (SELECT COUNT(*) FROM sys.columns WHERE object_id = OBJECT_ID('order_items')),
                    SYSUTCDATETIME(), NULL);
        </sql>

        <rollback>
            <sql>DELETE FROM dbo.tables WHERE name = 'order_items';</sql>
            <dropTable tableName="order_items"/>
        </rollback>
    </changeSet>

</databaseChangeLog>
